# Testcontainers will override these via @DynamicPropertySource
spring.datasource.url=jdbc:postgresql://localhost:5432/soundboard_test
spring.datasource.username=test
spring.datasource.password=test

spring.jpa.hibernate.ddl-auto=create-drop

logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=DEBUG

spring.devtools.restart.enabled=false

# This should be a directory that is created and deleted during tests
app.audio-storage.base-path=./test-audio-storage

security.require-https=false

app.jwt.secret=dGVzdC1zZWNyZXQta2V5LWZvci10ZXN0aW5nLW9ubHktMTIzNDU2Nzg5MA==

app.cors.allowed-origins=http://localhost:3000

# Admin configuration
app.admin.force-password-change=true
